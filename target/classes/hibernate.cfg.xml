<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings - SQLite Database -->
        <property name="connection.driver_class">org.sqlite.JDBC</property>
        <property name="connection.url">jdbc:sqlite:aeronautica.db</property>
        <property name="connection.username"></property>
        <property name="connection.password"></property>

        <!-- JDBC connection pool -->
        <property name="connection.pool_size">1</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.community.dialect.SQLiteDialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Update database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Mapping classes -->
        <mapping class="com.aeronautica.model.Aeronave"/>
        <mapping class="com.aeronautica.model.Mecanico"/>
        <mapping class="com.aeronautica.model.Pieza"/>
        <mapping class="com.aeronautica.model.Revision"/>
        <mapping class="com.aeronautica.model.Usuario"/>
    </session-factory>
</hibernate-configuration>